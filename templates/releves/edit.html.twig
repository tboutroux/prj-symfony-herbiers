{% extends 'base.html.twig' %}

{% block title %}Edit Releves{% endblock %}

{% block body %}

    <div class="container">

        <div class="update-releve">
        
            <h2>Modifier le Relevé</h2>

            {{ include('releves/_form.html.twig', {'button_label': 'Modifier'}) }}

            <div class="buttons">

                <a class="btn btn-secondary" href="{{ path('app_home') }}">Retour</a>
                {{ include('releves/_delete_form.html.twig') }}

            </div>

        </div>

    </div>
{% endblock %}
