<div class="delete-releve">
    <form method="post" action="{{ path('app_releves_delete', {'id': relefe.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce relevé ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relefe.id) }}">
        <button class="btn btn-danger">Supprimer</button>
    </form>
</div>
